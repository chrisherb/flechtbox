# flechtbox

Terminal-based groovebox.

## dependencies

for debian-based systems:

```bash
sudo apt install libportaudio2
```

## build dependencies

for debian-based systems:

```bash
sudo apt install portaudio19-dev libftxui-dev
```
